# 灵蕴 AssetFlow · 产品需求文档 v1.0

> 游戏科学研发生产中台 · 面向 3A 单机游戏资产管理与评审协同
> 项目代号：BMZ（黑神话新作）

---

## 目录

1. [产品概述](#1-产品概述)
2. [用户与场景](#2-用户与场景)
3. [信息架构](#3-信息架构)
4. [数据模型](#4-数据模型)
5. [功能规格（7 页面）](#5-功能规格)
6. [核心交互流程](#6-核心交互流程)
7. [验收标准](#7-验收标准)
8. [附录](#8-附录)

---

## 1. 产品概述

### 1.1 产品定位

灵蕴 AssetFlow 是为游戏科学（Game Science）黑神话新作（BMZ）团队设计的研发生产中台，聚焦**资产管理、任务协作、评审协同、风险监控**四大核心场景。

### 1.2 解决的痛点

| 痛点 | 现状 | 目标 |
|------|------|------|
| 资产版本混乱 | 文件名手动标记版本，缺乏版本链追溯 | 版本树可视化 + 关联任务自动索引 |
| 评审反馈断裂 | 微信群截图反馈 → 遗漏 → 反复沟通 | 在线批注 + 返修追踪 + 到期提醒 |
| 任务状态不可视 | Jira 任务与资产文件脱钩 | 看板拖拽 + 资产-任务双向关联 |
| 风险后知后觉 | PM 周报汇总才发现逾期/阻塞 | 实时风险仪表盘 + 类型分布分析 |

### 1.3 核心假设

- 首期覆盖角色美术、场景道具、特效音频、动画镜头、UI 蓝图五大资产类别
- 模拟数据量级：15 资产 / 20 任务 / 6 评审项 / 10 规范 / 5 风险项
- 首期 Web MVP 为单文件 HTML，零后端依赖

---

## 2. 用户与场景

### 2.1 核心 Persona

**陈磊** — 角色美术师，制作成员
- 日常：制作资产 → 提交评审 → 接收返修 → 迭代
- 痛点：不清楚自己被打了多少回返修、不记得哪个版本是"最新的可提交版本"
- 使用频率：每日多次

**王主美** — 主美，评审者
- 日常：评审资产 → 批注 → 通过/返修决策
- 痛点：评审积压看不到总量、返修到期没有提醒
- 使用频率：每日集中评审

**张 PM** — 项目经理
- 日常：盯进度、识别风险、推动阻塞解决
- 痛点：风险靠人肉汇总、逾期资产后知后觉
- 使用频率：每日晨会前 + 随时监控

### 2.2 用户故事（关键）

- 作为制作人员，我希望打开工作台就知道今天有多少任务、哪些被打回、哪些阻塞
- 作为评审者，我希望能集中看到待评审队列、在线批注、一键通过或返修
- 作为 PM，我希望能看到风险分布和趋势，点击直接定位到具体资产

---

## 3. 信息架构

```
灵蕴 AssetFlow
├── P1 工作台 Dashboard         ← 默认首页
├── P2 资产库 Asset Library
│   └── P3 资产详情 Asset Detail
├── P4 任务看板 Task Board
├── P5 评审中心 Review Center
├── P6 规范库 Spec Library
└── P7 项目风险 Risk Overview
```

### 3.1 全局导航

| 元素 | 说明 |
|------|------|
| Sidebar | 240px 固定侧边栏：品牌 Logo + 项目名称（BMZ）+ 6 模块导航 + 项目切换器 |
| Topbar | 64px 全局顶栏：搜索框 + 新建资产按钮 + 待处理统计 |
| Content | 自适应内容区，24px 页面边距 |

---

## 4. 数据模型

### 4.1 资产 Asset

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | 资产名（如"鬼将军"） |
| type | enum | 角色/怪物/场景/道具/特效/音频/关卡/UI/镜头/蓝图/动画/材质 |
| code | string | 资产编号（如 BMZ_Character_GhostGeneral_042） |
| version | string | 当前版本号（如 v1.8） |
| stage | enum | 概念期/制作中/待评审/返修中/已通过/已入库/阻塞/搁置 |
| owner | string | 负责人 |
| collaborators | string[] | 协作者列表 |
| risk | enum | high/mid/low |
| revisionCount | int | 累计返修次数 |
| deadline | date | 截止日期 |
| phase | string | 所属阶段（如 VS/Alpha/Beta） |

### 4.2 任务 Task

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| title | string | 任务标题 |
| assetId | string | 关联资产 ID |
| assetName | string | 关联资产名 |
| status | enum | 未开始/进行中/阻塞/待评审/已完成 |
| priority | enum | 高/中/低 |
| assignee | string | 执行人 |
| blockReason | string | 阻塞原因（仅阻塞状态） |
| deadline | date | 截止日期 |

### 4.3 评审 Review

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| assetId | string | 关联资产 ID |
| assetName | string | 资产名 |
| version | string | 评审版本 |
| stage | enum | 待评审/返修中/搁置 |
| reviewer | string | 评审人 |
| annotations | object[] | 批注列表 |
| revisionItems | object[] | 返修项列表 |
| status | enum | pending/approved/revision |

---

## 5. 功能规格

### 5.1 P1 工作台 Dashboard

**功能点**：
- 今日制作态势：4 统计卡（我的资产/我的任务/被打回项/阻塞项），每卡包含总计数量 + 子项提示
- 我的资产卡片列表：显示资产名/类型/版本/状态/返修次数/负责人
- 我的任务列表：显示标题/关联资产/优先级色条/状态标签
- 右侧待处理面板：返修中 + 阻塞项分列
- 点击资产/任务跳转到对应详情

**交互**：
- 统计卡 hover 上浮 + 数值放大
- 任务条目点击进入任务看板并定位

### 5.2 P2 资产库 Asset Library

**功能点**：
- 组合筛选：类型（角色·怪物/场景·道具/特效·音频/动画·镜头/UI·蓝图）+ 状态（8 种）
- 模糊搜索：按资产名或编号搜索
- 卡片网格：缩略图 + 名称/编号 + 状态标签 + 版本 + 返修次数 + 负责人
- 右侧统计：总计资产 / 待评审 / 高风险

**交互**：
- 筛选标签多选（类型 + 状态独立并行）
- 筛选无结果时显示空状态插画 + 提示文案
- 卡片 hover scale + 阴影，点击进入资产详情

### 5.3 P3 资产详情 Asset Detail

**功能点**：
- 三栏布局：预览区（左）+ 资产信息 + 版本与关联任务表（中）+ 评审与返修记录时间线（右）
- 预览区：SVG 线稿图标 + 网格线装饰 + 资产名称标注
- 版本表格：版本号/类型/状态/负责人/时间/描述，行点击展开变更详情
- 评审时间线：主美批注（朱砂边框）→ TA 检查（蓝边框）→ 复审记录（绿边框）
- 操作按钮：发起评审（金）+ 提交版本 + 删除资产（朱砂危险按钮）

**交互**：
- 删除资产触发二次确认弹窗，朱砂红色主题，需输入资产名确认
- 发起评审弹出确认 Modal
- 版本行 ▸ 箭头旋转展开/折叠

### 5.4 P4 任务看板 Task Board

**功能点**：
- 5 列看板：未开始 / 进行中 / 阻塞（红框强调） / 待评审 / 已完成
- 任务卡片：标题 + 关联资产 + 状态标签 + 优先级色条 + 复选框
- 拖拽换列：drag & drop API，从阻塞列拖出时清空阻塞原因

**交互**：
- 拖拽中：卡片旋转 + 放大 + 虚线边框
- 任务勾选：卡片右上角 SVG 复选框，已勾选变绿 + 标题删除线 + 半透明
- 阻塞卡片特殊样式：红色左边框 + 朱砂标签

### 5.5 P5 评审中心 Review Center

**功能点**：
- 左侧待评审队列：列表项显示资产名/版本/状态
- 中间预览区 + 材料批注列表
- 审批按钮：通过（金）/ 要求返修
- 右侧返修项面板：列出返修任务 + 负责人 + 截止时间

**交互**：
- 点击队列项切换评审对象，同步刷新预览区、批注列表、返修项面板
- 通过/返修后 toast 反馈

### 5.6 P6 规范库 Spec Library

**功能点**：
- 5 分类 Tab：美术规范(24篇) / 动画规范(16) / 性能预算(9) / 外包验收(12) / 案例复盘(7)
- 规范列表卡片：标题 + 适用标签 + 适用范围 + 更新日期
- 搜索 + 分类联动筛选
- 右侧统计卡：引用资产数 + 返修下降百分比

**交互**：
- Tab 切换金边高亮
- 卡片点击弹出规范详情 Modal
- 筛选无结果显示空状态

### 5.7 P7 项目风险 Risk Overview

**功能点**：
- 4 风险统计卡：逾期资产 7(+2) / 高返修资产 12 / 待评审积压 18 / 阻塞任务 3
- 风险清单表格：资产名/类型/风险等级标签/负责人/版本/原因描述（高风���红色文字）
- 右侧风险分布横向条形图：按类型展示风险资产数量

**交互**：
- 表格行点击跳转资产详情
- 条形图纯 CSS 实现，无图表库依赖

---

## 6. 核心交互流程

### 6.1 资产创建 → 评审 → 入库

```
新建资产 → 任务拆分 → 制作推进 → 提交评审 → 主美/TA 批注
                                              ↓
                                    通过 → 入库（结束）
                                    返修 → 制作人修改 → 再次提交评审
```

### 6.2 阻塞处理

```
任务进行中 → 依赖缺失/规范不明 → 标记阻塞（红框 + 原因描述）
                                  ↓
                        PM 介入 → 解决阻塞 → 拖回进行中列
```

### 6.3 危险操作防护

- 删除资产 → 二次确认弹窗（朱砂红色，需输入资产名）
- 通过评审 → 确认 Modal
- 要求返修 → 需填写返修原因

---

## 7. 验收标准

### 7.1 MVP v1

- [x] 7 个页面全部可访问，Hash Router 正常跳转
- [x] 模拟数据完整渲染（资产/任务/评审/规范/风险）
- [x] 资产库：类型+状态组合筛选 + 模糊搜索
- [x] 任务看板：5 列 drag & drop 换列
- [x] 评审中心：通过/返修按钮交互
- [x] 规范库：5 分类 Tab 切换
- [x] 项目风险：清单表格 + CSS 条形图
- [x] 删除资产二次确认弹窗

### 7.2 MVP v2（视觉+交互优化）

- [x] 12 种 SVG 线稿资产缩略图替换 emoji
- [x] 页面淡入过渡动画
- [x] 卡片/按钮/统计卡微交互（hover/press/drag）
- [x] 预览区网格线 + 暗角装饰
- [x] 统一滚动条样式 + 空状态 + 骨架屏
- [x] 评审队列切换实装渲染
- [x] 任务卡片勾选
- [x] 全局搜索下拉浮层
- [x] 规范详情弹窗
- [x] 版本表行展开/折叠
- [x] 响应式 1280px 断点

---

## 8. 附录

### 8.1 设计参考

- `design.md`：信息架构与页面设计说明
- `style-guide.md`：美术风格、色板、组件系统规范
- Figma 原型导出 PDF：10 页（3 页规范 + 7 页原型）

### 8.2 技术选型

| 决策 | 选择 | 原因 |
|------|------|------|
| 前端框架 | Vanilla JS | MVP 快速验证，零构建步骤 |
| 路由 | Hash Router | 单文件部署，无需服务器 |
| 样式 | CSS Variables | Design Token 系统，主题可切换 |
| 拖拽 | Native Drag & Drop API | 零依赖 |
| 图表 | CSS 实现 | 无第三方库，轻量 |
| 部署 | 单文件 HTML | 双击即用，GitHub Pages 友好 |

### 8.3 已知限制与后续规划

| 限制 | 规划 |
|------|------|
| 缩略图为 SVG 占位 | 接入真实 DCC 工具截图或 AI 生成概念图 |
| 无数据持久化 | 接入 localStorage 或后端 API |
| 无用户认证 | 后续接入 OAuth/SSO |
| 评审仅前端模拟 | 后续接入 WebSocket 实时协同批注 |
| 无通知系统 | 后续接入邮件/企微/飞书提醒 |

---

*文档版本 v1.0 · 2026.05.22 · 作者：钱天翼（Eric Qian）*
